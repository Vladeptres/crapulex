# Local Development Environment Configuration
# Used for day-to-day development

# MongoDB Configuration
MONGO_ROOT_USERNAME=admin
MONGO_ROOT_PASSWORD=dev_password
MONGO_DB_NAME=bourracho_dev

# Django Configuration
BOURRACHO_SECRET_KEY=django-insecure-local-development-key-not-for-production
DEBUG=true

# Redis Authentication (simple for local development)
REDIS_PASSWORD=dev_redis_pass

# Domain Configuration (relaxed for development)
ALLOWED_HOSTS=localhost,127.0.0.1,0.0.0.0
CORS_ALLOWED_ORIGINS=http://localhost:3000,http://localhost:5173,http://127.0.0.1:3000,http://127.0.0.1:5173

# AWS S3 Configuration (optional - use local storage)
S3_BUCKET_NAME=
S3_ACCESS_KEY_ID=
S3_SECRET_ACCESS_KEY=
