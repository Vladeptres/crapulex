{
    "build": {
      "builder": "NIXPACKS",
      "buildCommand": "uv sync --no-dev && uv run migration",
      "startCommand": "uv run uvicorn api.asgi:application --host 0.0.0.0 --port $PORT"
    }
  }
  