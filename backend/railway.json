{
    "build": {
      "builder": "NIXPACKS",
      "buildCommand": "uv sync --no-dev",
      "startCommand": "uv run migration && uv run uvicorn api.asgi:application --host 0.0.0.0 --port $PORT"
    }
  }
  